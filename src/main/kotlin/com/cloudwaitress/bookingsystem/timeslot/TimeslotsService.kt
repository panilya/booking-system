package com.cloudwaitress.bookingsystem.timeslot

import com.cloudwaitress.bookingsystem.booking.TimeSlot
import org.springframework.stereotype.Service
import java.time.LocalTime
import java.time.format.DateTimeFormatter

@Service
class TimeslotsService {

//    fun generateTimeslots(slotSizeInMinutes: Int, startTime: String, endTime: String): List<TimeSlot> {
//        val formatter = DateTimeFormatter.ofPattern("HH:mm")
//
//        val formattedStartTime = LocalTime.parse(startTime, formatter)
//        val formattedEndTime = LocalTime.parse(endTime, formatter)
//
//
//    }
}